const utils = require('./utils')
const { Map, toJS, fromJS } = require('immutable')
let oneBoard = utils.getOneBoard()

function getMinMax () {

}

function getBestMove (oneBoard, deepMove = 10) {
}


getBestMove()
